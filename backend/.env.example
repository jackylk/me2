# Me2 环境变量配置示例
# 复制此文件为 .env 并填入真实的值

# ============ 应用配置 ============
APP_NAME=Me2
APP_VERSION=0.1.0
DEBUG=true
SECRET_KEY=change-me-in-production-use-random-string

# ============ 数据库配置 ============
# 使用与 NeuroMemory 相同的数据库
DATABASE_URL=postgresql+asyncpg://neuromemory:neuromemory@localhost:5432/neuromemory

# ============ JWT 认证 ============
JWT_SECRET=change-me-in-production-use-random-string-minimum-32-characters
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=7

# ============ LLM 配置 ============
# DeepSeek API（需要申请）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# ============ Embedding 配置 ============
# 本地模型，首次运行会自动下载
# 可选模型：
# - BAAI/bge-small-zh-v1.5 (推荐，中文优化，512 维，约 100MB)
# - BAAI/bge-base-zh-v1.5 (更准确，768 维，约 400MB)
# - sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 (多语言，384 维，约 450MB)
EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5

# ============ NeuroMemory 配置 ============
# 每 N 条消息自动提取记忆
NEUROMEMORY_EXTRACTION_INTERVAL=10

# 每 N 次提取后自动反思（生成洞察）
NEUROMEMORY_REFLECTION_INTERVAL=50

# 是否启用知识图谱
NEUROMEMORY_GRAPH_ENABLED=true

# ============ CORS 配置 ============
# 前端地址，多个用逗号分隔
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============ 主动关心 ============
# 每 N 秒检查一次（未来功能）
PROACTIVE_CHECK_INTERVAL=3600
