# FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy>=2.0.25
asyncpg>=0.30.0
alembic==1.13.1

# NeuroMemory
neuromemory==0.4.2
boto3>=1.34.0  # neuromemory S3 storage dependency
email-validator>=2.0.0  # neuromemory pydantic email fields

# LLM
openai>=1.50.0  # DeepSeek 使用 OpenAI 兼容接口
httpx>=0.26.0

# Embedding (本地模型，可选 - 不安装则自动使用远程 API)
# sentence-transformers==2.3.1
# torch>=2.0.0
# 如需本地 embedding，手动安装: pip install sentence-transformers torch

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # 固定版本，避免与 passlib 兼容问题

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
aiofiles==23.2.1

# Testing
pytest==8.0.0
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-mock==3.12.0
faker==22.6.0
